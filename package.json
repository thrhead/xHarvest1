{
  "name": "ekim-hasat-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "cms",
    "mobile"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=cms",
    "dev:mobile": "npm run start --workspace=mobile",
    "build": "npm run build --workspace=cms",
    "start": "npm run start --workspace=cms",
    "seed": "npm run seed --workspace=cms",
    "lint": "npm run lint --workspace=cms"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4"
  },
  "overrides": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  }
}
